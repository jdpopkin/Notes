<% if commentable.class == Song %>
  <%= user.username %> added a new comment to <%= commentable.title %> <%= time %> ago.
<% else %>
  <%= user.username %> added a new comment to a note on <%= commentable.song.title %> <%= time %> ago.
<% end %>